# The Neat Company NM-1000 mobile sheetfed scanner.
# Grant the active desktop user access (uaccess) so SANE / SimpleScan can open it.
SUBSYSTEM=="usb", ENV{DEVTYPE}=="usb_device", ATTRS{idVendor}=="1f44", ATTRS{idProduct}=="0001", MODE="0664", ENV{libsane_matched}="yes", TAG+="uaccess"
